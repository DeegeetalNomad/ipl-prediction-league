[build]
  command   = "npm run build"
  publish   = "dist"

[functions]
  directory = "netlify/functions"

[functions."sync-results"]
  schedule = "0 18 * * *"

# ── NEW: SPA Redirect Rule to fix the 404 Admin error ──
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
